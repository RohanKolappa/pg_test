BarcoSDK enables developers to integrate with the Barco Collaboration solution.

Following is the folder structure:
BarcoSDK
 + player (contains SDK player that can be integrated by developers to create custom user interfaces)
  + doc
  + src
  
 + rest (contains the REST API and tester)
  + doc
  + src
  
 + xmpp (contains XMPP API, XMPP client application and sample JAVA code to communicate with XMPP client application)
  + doc
  + src

